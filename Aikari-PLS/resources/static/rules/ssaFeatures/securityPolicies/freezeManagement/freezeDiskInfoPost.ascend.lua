-- Rule Prop Area --

local cjsonUtil = require("cjson")

ruleProp = {
    ruleType = "ASCEND",
    endpointType = nil,
    featureType = nil,
    featureName = nil,
    ruleName = "freezeDiskInfoPost.ascend",
    ruleSide = "REQ",
    enabledBy = nil,
    configKey = nil
}

-- Impl Area --

function onRecv(packet)

end
