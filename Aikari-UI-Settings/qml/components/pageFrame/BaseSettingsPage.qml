import QtQuick
import QtQuick.Controls.Basic

Item {
    id: baseSettingsPageRoot

    // width: <<< SET
    height: parent.height

    ScrollView {
        id: baseSettingsPageScrollView
        anchors.fill: parent
    }
}
